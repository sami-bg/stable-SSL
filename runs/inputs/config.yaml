# config.yaml
optim:
  epochs: 25
  batch_size: 64
  
model:
  architecture: "resnet50"

log:
  folder: "./new_logs"
  eval_only: false
  add_version: false
  log_level: 10  # logging.DEBUG
  checkpoint_frequency: 5

hardware:
  seed: 123
  float16: false
  gpu: 1
  world_size: 1