# Pin key packages for deterministic integration tests.
# These versions must match the local dev environment so that
# seeded loss values stay consistent across CI and local runs.
torch==2.11.0
torchvision==0.26.0
numpy==2.1.2
timm==1.0.15
transformers==4.55.4
lightning==2.6.1
# Pin the HF + fsspec stack to a known-good combination. The bad-glob
# bug from huggingface_hub <0.36.2 (constructing patterns like
# ``train[-._ 0-9/]**``) is fixed in 0.36.2, but fsspec >=2026.3 tightened
# its strict validator further and re-trips on patterns the older
# validator accepted. 2026.2.0 + 0.36.2 is the verified-passing combination.
fsspec==2026.2.0
huggingface_hub==0.36.2
